# CineMatch
개인 영화 취향 분석 및 맞춤형 영화 추천 웹 애플리케이션

## 1. 프로젝트 개요

### 1.1 프로젝트명
CineMatch (가칭)

### 1.2 프로젝트 소개
CineMatch는 사용자가 시청한 영화와 개인 평점을 기반으로 영화 취향을 분석하고, 이를 바탕으로 새로운 영화를 추천하는 개인화 영화 추천 웹 애플리케이션이다.

사용자는 영화 검색을 통해 자신이 시청한 영화를 등록하고 개인 평점을 남길 수 있다. 서비스는 사용자의 시청 기록, 선호 장르 및 평가 정보를 분석하여 사용자가 아직 시청하지 않은 영화를 추천한다.

추후 MCP(Model Context Protocol)와 LLM을 연동하여 사용자가 자연어로 영화 추천 조건을 입력하면, AI가 사용자의 영화 취향과 요청 조건을 종합적으로 분석하여 맞춤형 영화를 추천하는 기능으로 확장한다.

### 1.3 프로젝트 포지셔닝
본 프로젝트는 **프론트엔드(React + TypeScript) 역량 증명을 최우선 목표**로 하며, 백엔드/추천 로직은 이미 검증된 자산(MovieLens 협업 필터링 과제, Project Regalia의 Spring Boot 경험)을 재사용하는 방식으로 진행한다. 백엔드에서 새로운 개념을 익히는 데 시간을 쓰기보다, 프론트엔드 구현의 완성도에 집중한다.

---

## 2. 프로젝트 목적

최근 OTT 서비스와 영화 플랫폼에서는 수많은 영화 콘텐츠가 제공되고 있어 사용자가 자신의 취향에 맞는 콘텐츠를 직접 탐색하는 데 어려움이 있다.

기존 영화 추천 서비스는 인기 영화나 단순 장르 기반 추천을 제공하는 경우가 많다.

본 프로젝트에서는 사용자의 다음 데이터를 활용한다.

- 시청한 영화
- 사용자 개인 평점
- 선호 장르
- 영화 메타데이터

이를 기반으로 사용자별 영화 취향을 분석하고 개인화된 영화 추천 기능을 제공하는 것을 목표로 한다.

또한 단순한 영화 추천 서비스에 그치지 않고 MCP와 LLM을 활용하여 자연어 기반 영화 추천 기능을 구현하는 것을 확장 목표로 한다.

---

## 3. 사용자 식별 방식 (신규)

로그인 기능은 선택 사항(우선순위 낮음)으로 두되, "누구의 시청 기록인지" 구분은 MVP 단계부터 필요하다. 이를 위해 **익명 사용자 ID** 방식을 사용한다.

- 최초 접속 시 브라우저에서 UUID를 생성하여 `localStorage`에 저장
- 이후 모든 API 요청 시 이 ID를 헤더(`X-User-Id`) 또는 요청 파라미터로 전달
- 백엔드 DB의 시청기록/평점 테이블은 이 익명 ID를 `user_id` 컬럼으로 사용
- 추후 로그인 기능을 추가할 경우, 이 익명 ID 자리를 실제 사용자 인증 정보로 대체

이 방식은 8/19~8/21 설계 단계에서 확정하고, 프론트/백엔드 API 스펙에 처음부터 반영한다.

---

## 4. 주요 사용자 시나리오

### 시나리오 1 — 영화 검색 및 시청 기록 등록

```
사용자는 영화 제목을 검색한다.

"인터스텔라"
        ↓
영화 검색 결과
        ↓
영화 상세 페이지
        ↓
[봤어요]
        ↓
개인 평점 등록
```

등록된 영화는 사용자의 시청 기록에 저장된다.

### 시나리오 2 — 내 영화 목록 관리

사용자는 자신이 시청한 영화 목록을 확인할 수 있다.

```
내가 본 영화

인터스텔라       ★★★★★
듄               ★★★★☆
다크 나이트      ★★★★★
라라랜드         ★★★★☆
```

사용자는 다음 기능을 사용할 수 있다.

- 영화 목록 조회
- 개인 평점 수정
- 시청 기록 삭제
- 장르별 필터링
- 평점순 정렬

### 시나리오 3 — 개인화 영화 추천

서비스는 사용자의 영화 기록을 분석한다.

예를 들어:

```
사용자 영화 기록

인터스텔라      ★★★★★
듄              ★★★★★
테넷            ★★★★☆
라라랜드        ★★★★☆
```

취향 분석:

```
선호 장르
1. SF
2. 드라마
3. 액션

선호 감독
1. Christopher Nolan
2. Denis Villeneuve
```

이를 기반으로 사용자가 아직 보지 않은 영화를 추천한다.

```
추천 영화

1. 블레이드 러너 2049
   SF / 드라마

추천 이유
→ 사용자가 높은 평점을 준
   듄과 유사한 SF 영화입니다.
```

### 시나리오 4 — 자연어 기반 영화 추천 (확장, 선택 기능)

사용자가 AI에게 자연어로 요청한다.

```
"오늘 밤에 볼 영화 추천해줘. 너무 무거운 건 싫고 2시간 정도면 좋겠어."
```

LLM은 사용자의 요청을 분석한다.

```
조건 분석

- 가벼운 분위기
- 상영 시간 약 2시간
```

필요한 정보를 MCP Tool을 통해 조회한다.

```
사용자 영화 기록 조회
        ↓
영화 후보 검색
        ↓
시청한 영화 제외
        ↓
조건에 맞는 영화 추천
```

최종적으로 추천 결과와 추천 이유를 제공한다.

---

## 5. 핵심 기능

### 5.1 영화 탐색
- 영화 제목 검색
- 인기 영화 조회
- 최신 영화 조회
- 영화 상세 정보 조회
- 포스터 및 기본 정보 제공

영화 데이터는 외부 영화 데이터 API(TMDB)를 활용하며, **프론트엔드는 TMDB를 직접 호출하지 않고 Spring Boot 백엔드를 경유**한다. (API 키 보호 및 응답 스펙 일원화 목적)

### 5.2 시청 기록 관리

사용자는 자신이 시청한 영화를 등록할 수 있다.

관리 정보:
- 영화
- 개인 평점
- 시청 여부
- 등록 날짜

기능:
- 시청 영화 등록
- 평점 등록 및 수정
- 시청 기록 삭제
- 시청 영화 목록 조회

### 5.3 취향 분석

사용자의 시청 기록과 평점을 기반으로 다음 정보를 분석한다.

- 선호 장르
- 선호 감독
- 평균 평점
- 장르별 시청 비율
- 높은 평점을 준 영화

분석 결과는 사용자에게 시각적으로 제공한다.

### 5.4 영화 추천

추천 과정:

```
사용자 시청 기록
        ↓
사용자 평점 분석
        ↓
선호 장르 분석
        ↓
추천 후보 영화 검색
        ↓
이미 시청한 영화 제외
        ↓
추천 점수 계산
        ↓
상위 영화 추천
```

초기 추천 알고리즘은 복잡한 머신러닝 대신 콘텐츠 기반 추천 방식을 적용한다.

**추천 점수 계산식 (가중치 확정, 신규)**

```
추천 점수 = (장르 선호도 × 0.4)
          + (선호 감독 일치 × 0.3)
          + (사용자 평점 정규화 × 0.2)
          + (유사 영화 여부 × 0.1)
```

- **장르 선호도 (40%)**: 사용자가 시청한 영화의 장르 분포 대비, 후보 영화가 겹치는 장르 비율
- **선호 감독 일치 (30%)**: 후보 영화 감독이 사용자의 상위 선호 감독 리스트에 포함되는지 여부 (가중치 큰 항목으로, cold-start 상황에서도 뚜렷한 추천 근거를 만들어줌)
- **사용자 평점 정규화 (20%)**: 유사한 영화에 사용자가 부여했던 평점을 0~1로 정규화
- **유사 영화 여부 (10%)**: 장르/키워드 기반 콘텐츠 유사도 (TMDB 메타데이터 활용)

각 항목은 0~1로 정규화 후 가중합하며, MovieLens 과제에서 사용한 유사도 계산 로직을 재사용한다. 이를 통해 추천 결과를 직접 계산하고 추천 근거를 제공한다.

---

## 6. 추가 기능 (시간 여유가 있을 경우 구현)

**우선순위 중간**
- 영화 찜 목록
- 장르별 영화 탐색
- 사용자 취향 분석 차트
- 감독 및 배우 선호도 분석
- 추천 영화 저장

**우선순위 낮음**
- 회원가입 및 로그인 (실제 인증 시스템으로 익명 ID 대체)
- 다른 사용자와 영화 취향 비교
- 영화 리뷰 작성
- OTT 플랫폼별 시청 가능 여부

---

## 7. MCP 및 LLM 확장 기능 (선택 기능)

MCP는 영화 추천 서비스의 기존 기능을 LLM이 호출할 수 있도록 연결하는 역할을 한다.

예상 Tool:

- `get_user_watched_movies()` — 사용자의 시청 기록 조회
- `get_user_preferences()` — 사용자의 영화 취향 분석 결과 조회
- `search_movies()` — 조건에 맞는 영화 검색
- `recommend_movies()` — 사용자의 취향과 조건을 기반으로 영화 추천

전체 흐름:

```
사용자
   ↓
자연어 요청

"주말에 가볍게 볼 SF 영화 추천해줘.
 내가 이미 본 건 제외해줘."

   ↓
LLM
   ↓
요청 조건 분석
   ↓
MCP Tool 호출
   ↓
시청 기록 조회
영화 후보 검색
추천 기능 호출
   ↓
추천 결과
   ↓
사용자에게 자연어로 설명
```

> MCP Tool은 새로운 추천 로직을 만드는 것이 아니라, 5.4에서 구현한 추천 API를 그대로 감싸서 노출하는 방식으로 구현한다. (WareLens에서 FastAPI CLIP 엔드포인트를 MCP로 감싼 경험 재사용)

---

## 8. 시스템 구성

```
┌──────────────────────────────┐
│        React + TypeScript     │
│                                │
│  영화 탐색                     │
│  내 영화 목록                  │
│  취향 분석                     │
│  영화 추천                     │
│  AI 채팅 (확장)                │
└───────────────┬──────────────┘
                │ REST API (+ X-User-Id 헤더)
                ▼
┌──────────────────────────────┐
│         Spring Boot           │
│                                │
│  영화 관리 API                 │
│  사용자 시청 기록 관리          │
│  추천 서비스                   │
│  외부 API 연동 (TMDB 프록시)   │
└───────────┬─────────┬────────┘
            │         │
            ▼         ▼
      ┌──────────┐  ┌───────────┐
      │ Database │  │ Movie API │
      │ (MySQL)  │  │ (TMDB)    │
      └──────────┘  └───────────┘

확장
        Spring Boot / MCP Server
                    │
                    ▼
                   LLM
```

---

## 9. 기술 스택

| 구분 | 기술 |
|---|---|
| Frontend | React |
| Language | TypeScript |
| Build Tool | Vite |
| Backend | Spring Boot |
| Language | Java |
| Database | MySQL |
| ORM | Spring Data JPA |
| External API | TMDB (영화 데이터) |
| 사용자 식별 | 익명 UUID (localStorage) |
| AI/LLM (확장) | 로컬 LLM |
| MCP (확장) | Model Context Protocol |

여기서 중요한 건 처음부터 전부 구현하지 않는 것.

---

## 10. MVP 범위 (9월 18일까지 필수)

**필수 — Frontend**
- [ ] 영화 목록
- [ ] 영화 검색
- [ ] 영화 상세 페이지
- [ ] 본 영화 등록
- [ ] 개인 평점 입력
- [ ] 내가 본 영화 목록

**필수 — Backend**
- [ ] REST API
- [ ] Spring Boot 연동
- [ ] 익명 사용자 ID 처리 (헤더 기반)
- [ ] 시청 기록 저장
- [ ] 영화 데이터 API(TMDB) 연동 (백엔드 프록시)

**필수 — Recommendation**
- [ ] 사용자 평점 분석
- [ ] 선호 장르 분석
- [ ] 가중치 기반 추천 점수 계산 (장르 40% / 감독 30% / 평점 20% / 유사도 10%)
- [ ] 이미 본 영화 제외
- [ ] 추천 영화 제공
- [ ] 추천 이유 제공

**선택**
- [ ] 취향 분석 차트
- [ ] 로그인 (실 사용자 인증)
- [ ] MCP
- [ ] 로컬 LLM
- [ ] 자연어 영화 추천

MCP + LLM은 선택 기능으로 둔다. 한 달 프로젝트에서 필수 기능으로 넣었다가 추천 시스템도 프론트도 애매하게 끝나는 것보다, 일반 추천 서비스가 완성된 상태에서 붙이는 것이 낫다.

---

## 11. 개발 일정

### 8/19 ~ 8/21 (수~금) — 기초 설계 및 React/TypeScript
- 프로젝트 생성 (Vite + React + TS)
- 익명 사용자 ID 방식 확정 및 구현
- React 구조 학습, TypeScript 타입 정의
- 화면 구성 (라우팅 포함)
- Mock Data 기반 영화 목록 구현

**목표**: API 없이도 영화 목록 UI가 동작.

### 8/22 ~ 8/26 (토~수) — 영화 탐색 기능
- 영화 검색
- 영화 목록
- 영화 상세 페이지
- 외부 영화 API(TMDB) 연동 방식 설계 (백엔드 경유 원칙 반영)

**목표**: 실제 영화 데이터를 검색하고 상세 정보를 볼 수 있음.

### 8/27 ~ 9/2 (목~수, 7일 — 최대 위험 구간) — Spring Boot + DB
- **8/27~8/28**: React ↔ Spring Boot 최소 연동(Hello World) 및 CORS 설정 최우선 확인
- Spring Boot 프로젝트 생성, REST API 구현
- DB 설계 (익명 user_id 포함)
- TMDB 프록시 API 구현
- 시청 기록 저장 API

**목표**: 본 영화와 개인 평점을 저장할 수 있음.

### 9/3 ~ 9/7 (목~월) — 영화 기록 관리
- 내가 본 영화 목록
- 평점 수정
- 영화 삭제
- 정렬 및 필터

**목표**: 사용자 영화 기록 관리 완성.

### 9/8 ~ 9/12 (화~토) — 추천 시스템
- 선호 장르/감독 분석
- 가중치 기반 추천 점수 계산 구현 (MovieLens 과제 로직 이식)
- 추천 후보 선정, 이미 본 영화 제외
- 추천 결과 UI (추천 이유 카드 포함)

**목표**: AI 없이도 개인화 추천이 동작.

### 9/13 ~ 9/16 (일~수) — 추가 기능 및 MCP 도전
우선순위:
1. 버그 수정
2. UI 개선
3. 취향 분석 차트
4. MCP
5. 로컬 LLM

### 9/17 (목) — 최종 테스트
- 전체 기능 테스트
- 예외 처리
- UI 정리
- README 작성
- 프로젝트 구조 정리

### 9/18 (금) — 최종 마감
